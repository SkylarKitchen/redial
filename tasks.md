# Sandcastle JSDoc sweep — src/overlay/core/

## Tasks

- [x] Add a JSDoc comment to every exported function in src/overlay/core/scrubState.ts describing what it does, its parameters, and its return value. Do not change any logic. Run `npm run typecheck` before finishing.
- [x] Add JSDoc comments to the exported NavigationHistory class (and its public methods) and the computePanelMaxHeight function in src/overlay/core/navigationHistory.ts, describing parameters and return values. Do not change any logic. Run `npm run typecheck` before finishing.
- [x] Add a JSDoc comment to the exported onHmrUpdate function in src/overlay/core/hmr.ts describing what it does, its parameter, and its return value. Do not change any logic. Run `npm run typecheck` before finishing.
