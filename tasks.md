# Sandcastle smoke-test tasks

## Tasks

- [ ] Add JSDoc comments to every exported function in src/overlay/core/infer.ts describing its parameters and return value. Do not change any logic. Run `npm run typecheck` before finishing.
- [ ] Add JSDoc comments to every exported function in src/overlay/core/apply.ts describing its parameters and return value. Do not change any logic. Run `npm run typecheck` before finishing.
