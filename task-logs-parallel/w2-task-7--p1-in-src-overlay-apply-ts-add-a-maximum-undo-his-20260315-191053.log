[WARN] Fast mode is not available in the Agent SDK. Using Opus 4.6.
All 58 tests pass (55 existing + 3 new).

`★ Insight ─────────────────────────────────────`
- The `MAX_UNDO` constant and trimming logic were **already implemented** across all 4 push sites (`applyInlineStyle`, `endBatch`, `removeCustomProperty`, `pushDomMove`). The change here was just exporting the constant and adding test coverage.
- The test uses custom properties (`--t0` through `--t249`) because each unique property name creates a separate undo entry — avoiding the coalescing logic that merges consecutive same-prop edits.
- The "oldest entries discarded" test verifies correctness by checking that `--t0` (trimmed) retains its value while `--t50` (oldest surviving entry) gets properly undone.
`─────────────────────────────────────────────────`

**Summary:**
- **Asked:** Add `MAX_UNDO = 200` cap on undo history with a test proving 250 changes are capped at 200
- **Did:** The cap and trimming logic already existed in `core/apply.ts:187`. Exported `MAX_UNDO` and added 3 tests: stack capped at 200 after 250 changes, oldest entries are the ones discarded, and batch entries respect the cap. All 58 tests pass.
