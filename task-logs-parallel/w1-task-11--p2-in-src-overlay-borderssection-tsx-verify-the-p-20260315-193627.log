[WARN] Fast mode is not available in the Agent SDK. Using Opus 4.6.
The only failure is a pre-existing one in `cssLayerSpecificity.test.ts` (expects built CSS to exist, which is a build artifact issue — not related to our changes). Our new 9 tests all pass, and all other 2278 tests continue passing.

`★ Insight ─────────────────────────────────────`
**The per-side border controls work correctly.** Here's what the tests verified:

1. **Width sync**: Switching to "Top" shows `width=2`, "Bottom" shows `width=1`, "Right" shows `width=3`, "Left" shows `width=0` — all matching the per-side values set on the element.

2. **Style sync**: Switching to "Top" shows `style=solid`, "Bottom" shows `style=dashed`, "Right" shows `style=dotted`, "Left" shows `style=none` — the `IconButtonGroup` correctly marks the active style via `aria-checked`.

3. **Property targeting**: When you modify the width while "Top" is selected, `apply()` is called with `"border-top-width"` (not the shorthand `"border-width"`). Same for "Bottom" → `"border-bottom-width"`.

The implementation is clean: the `useEffect` at line 148 depends on `[borderSide, element]`, so it fires whenever the tab changes. It reads fresh computed styles via `getPropertyValue(`border-${side}-style`)` for the specific side, correctly differentiating between "all" mode (shorthand) and per-side mode (longhand properties). The `borderProp()` helper at line 165 ensures all handlers target the right CSS property.
`─────────────────────────────────────────────────`

---
**Asked:** Verify per-side border controls show correct style/width/color when different sides have different values
**Did:** Read `BordersSection.tsx` implementation and existing tests, wrote 9 behavioral tests in `bordersPerSide.test.ts` that render the component with mixed borders (top: 2px solid red, bottom: 1px dashed blue, etc.) and verify switching tabs displays correct values. All 9 tests pass — the per-side controls work correctly.
---
