[WARN] Fast mode is not available in the Agent SDK. Using Opus 4.6.
All 28 tests pass. Here's what the test file covers across 5 `describe` blocks:

`★ Insight ─────────────────────────────────────`
**Test structure maps directly to the 5 spec requirements:**

1. **Add Layer Flow** (3 tests) — Verifies layer creation produces valid `BackgroundLayer` objects, prepends new layers to the front (topmost = frontmost stacking), and generates unique IDs. Uses a `makeLayer` helper that mirrors the internal `makeDefault()` factory.

2. **Layer Types** (6 tests) — Confirms `BackgroundLayerType` is exactly `"color" | "gradient" | "image"`, then validates each type's data shape: color layers have hex strings, gradient layers have `type/angle/stops` (with ≥2 stops in 0-100 range), and image layers have `url/size/position/repeat/attachment`. Also verifies all three gradient subtypes (linear/radial/conic).

3. **Gradient Editor** (7 tests) — Tests `buildGradientCSS()` directly: linear uses `Ndeg` syntax, radial uses `circle` (no angle), conic uses `from Ndeg`. Also validates angle range 0-360, stop sorting by position, minimum 2-stop output, and multi-stop inclusion.

4. **Background Clip** (5 tests) — Asserts `BG_CLIP_OPTIONS` has 4 entries including `"text"` (for gradient text effects) alongside the standard box values, with no duplicates.

5. **Blend Modes** (7 tests) — Verifies `BLEND_MODE_OPTIONS` has exactly 16 entries matching the CSS spec order (normal through luminosity), starts with `"normal"` as default, labels are title-cased, and no duplicates exist.
`─────────────────────────────────────────────────`

---
**Asked:** Write P1 acceptance tests for backgrounds section covering add-layer, layer types, gradient editor, bg-clip text, and 16 blend modes
**Did:** Created `src/overlay/__tests__/backgroundsSection.test.ts` with 28 tests across 5 describe blocks — all passing. Tests verify against actual exports from `BackgroundLayerList`, `GradientEditor`, and `panelConstants`.
---
