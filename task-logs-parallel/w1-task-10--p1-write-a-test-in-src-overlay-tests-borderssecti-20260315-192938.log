[WARN] Fast mode is not available in the Agent SDK. Using Opus 4.6.
1754 pass, 1 pre-existing failure (a CSS build test unrelated to our changes). Our 23 new tests all green.

`★ Insight ─────────────────────────────────────`
The test file uses **two complementary testing strategies**:
1. **DOM rendering tests** (`createRoot` + `act`) — verify the component renders correct ARIA attributes, mode toggles show/hide the corner grid, and side-tab clicks produce the right `apply()` calls with property names like `border-top-style` vs `border-style`
2. **Handler logic tests** (pure functions + mocked `apply`) — verify that `handleRadiusAllChange` writes to all 4 corner properties and that `convertUnit` correctly transforms between `px`/`em`/`rem`

The `%` unit test caught a real gap — `BORDER_UNITS` was the only unit array missing `%`, despite `border-radius: 50%` being one of the most common CSS patterns. The fix was a single-character addition.
`─────────────────────────────────────────────────`

Here's what the test file covers across its 23 tests:

| Group | Tests | What it verifies |
|-------|-------|-----------------|
| Side selector tabs | 4 | Radiogroup renders 5 options, "All" is default, clicking a tab toggles `aria-checked` |
| All mode applies to all sides | 3 | "All" → `border-style`, "Top" → `border-top-style`, all 5 sides map correctly |
| Radius linked/unlinked mode | 6 | Equal corners → single mode, differing → individual, toggle shows/hides 4-corner grid |
| Handler logic | 2 | Single-mode writes all 4 corners, individual-mode writes 1 corner |
| Radius unit support | 8 | `BORDER_UNITS` has `px`, `%`, `em`, `rem`; conversion math; bulk corner apply on unit change |

**Implementation fix**: Added `%` to `BORDER_UNITS` in `panelConstants.tsx` — consistent with how `SIZE_UNITS_H`, `LAYOUT_UNITS`, and `SPACING_UNITS` already include `%`.

---
**Asked:** Write P1 acceptance tests for BordersSection covering side selector tabs, all-mode application, radius linked/unlinked toggle, and px/% unit support.
**Did:** Created `bordersSection.test.ts` with 23 tests (all passing). Fixed `BORDER_UNITS` to include `%` (was missing unlike every other unit array). Updated `panelConstants.test.tsx` length assertion. Full suite: 1754 pass.
---
