## Summary

<!-- What does this PR do, and why? -->

Fixes #

## How it was verified

<!-- Commands run and their results. For bug fixes: point at the failing test
     you wrote first (the repo norm — the fix isn't done until it passes). -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (bug fixes include a regression test written first)
- [ ] `npm run lint` passes (no hardcoded hex — tokens come from `theme.ts`)
- [ ] Docs updated if behavior or structure changed (README / ARCHITECTURE / `src/overlay/DIRECTORY.md`)
